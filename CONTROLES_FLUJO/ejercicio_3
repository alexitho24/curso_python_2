# Pedir al usuario que ingrese un número entero positivo
numero = int(input("Por favor, ingresa un número entero positivo: "))

# Crear una lista con los números desde el ingresado hasta cero
cuenta_atras = list(range(numero, -1, -1))

# Imprimir la cuenta atrás separada por comas
print(*cuenta_atras,sep=",")